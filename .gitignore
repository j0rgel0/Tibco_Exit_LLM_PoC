# === Entorno Virtual de Python ===
# Ignorar la carpeta del entorno virtual para no subir librerías al repositorio.
venv/
.venv/

# === Archivos de Configuración Sensibles ===
# Ignorar el archivo .env que contiene la API Key.
# ¡NUNCA subas tus claves secretas a un repositorio público!
.env

# === Archivos Intermedios y de Salida ===
# Estos directorios se generan cada vez que se ejecuta el pipeline.
# No deben formar parte del control de versiones.
2_intermediate_data/
3_output_documentation/

# === Base de Datos Vectorial ===
# La base de datos de ChromaDB se genera localmente y puede ser muy grande.
vector_db/

# === Archivos de Caché de Python ===
# Ignorar las carpetas de caché que Python genera automáticamente.
__pycache__/
*.pyc
*.pyo
*.pyd

# === Archivos de IDEs y Editores ===
# Configuraciones específicas de editores de código.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# === Archivos del Sistema Operativo ===
# Archivos generados por el sistema operativo que no son parte del proyecto.
.DS_Store
Thumbs.db

# === Archivos de Dependencias de Python (Opcional) ===
# Si usas pip-tools o poetry, puedes ignorar los archivos de bloqueo generados.
# requirements.txt  <- NO ignorar si lo usas para definir dependencias.
# Pipfile.lock
# poetry.lock

# === Archivos de Registro (Logs) ===
# Si tu aplicación genera archivos de log, es bueno ignorarlos.
*.log
logs/

# === Archivos de Construcción y Distribución ===
# Si en el futuro decides empaquetar este proyecto.
/dist/
/build/
*.egg-info/