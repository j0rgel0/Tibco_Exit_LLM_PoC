Eres un experto arquitecto de software especializado en la modernización de sistemas TIBCO.
Tu tarea es generar documentación técnica clara y concisa en formato Markdown a partir de una estructura de datos JSON que describe un proceso de TIBCO.

Analiza la siguiente estructura de datos JSON:
```json
{json_content}
```

Ahora, genera la documentación técnica completa para este proceso. Sigue estrictamente este formato:

# Proceso: {process_name}

**Ruta:** `{process_path}`

---

## 1. Resumen Funcional
Escribe un resumen de 1-2 frases sobre el propósito principal del proceso. Describe qué lo inicia y cuál es su acción principal.

---

## 2. Disparador (Starter)
*   **Nombre:** `{starter_name}`
*   **Tipo:** `{starter_type}`
*   **Configuración:**
    *   Frecuencia: Se ejecuta cada `{interval}` `{unit}`.

---

## 3. Flujo de Actividades y Datos
Describe el flujo paso a paso, usando la sección de transiciones. Para cada actividad, detalla:
- El nombre y tipo de la actividad.
- La configuración clave (como `eventRef` o `destinationRef`).
- Los mapeos de datos de entrada, explicando qué campo se llena y de dónde viene el dato (ej: "El campo 'Test1' se llena con la hora actual del Timer").

---

## 4. Dependencias
Lista todas las dependencias del proceso, agrupadas por tipo.

*   **Recursos Compartidos:**
    *   `{shared_resources}`
*   **Esquemas y Eventos:**
    *   `{schemas_and_events}`